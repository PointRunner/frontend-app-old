import styled from "styled-components";
import variables from "../../../../variables";

export const SelectPointText = styled.h1`
    color: ${variables.darkColor};
    text-align: center;
    font-size: 8vw;
` 
